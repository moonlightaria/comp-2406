html
    head
        title "Cards"
        meta(charset="UTF-8")
    body()
        h1 List of Cards
        each val in cards
            a(href=`http://localhost:3000/cards/` + val._id)  #{val.name}
            br